name: Build & Test

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager "platforms;android-29"

      - name: Compile library
        run: |
          ANDROID_JAR="$ANDROID_HOME/platforms/android-29/android.jar"
          mkdir -p build/classes
          javac --release 8 \
            -classpath "$ANDROID_JAR" \
            -d build/classes \
            java/io/battlewithbytes/ucb/*.java
          cd build/classes
          jar cf ../ucblib.jar io/

      - name: Run tests
        run: |
          ANDROID_JAR="$ANDROID_HOME/platforms/android-29/android.jar"
          mkdir -p build/test
          javac --release 8 \
            -classpath "build/ucblib.jar:$ANDROID_JAR" \
            -d build/test \
            test/UcbFrameTest.java
          java -classpath "build/ucblib.jar:build/test" UcbFrameTest

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: ucblib
          path: build/ucblib.jar

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager "platforms;android-29"

      - name: Build JAR
        run: |
          ANDROID_JAR="$ANDROID_HOME/platforms/android-29/android.jar"
          mkdir -p build/classes
          javac --release 8 \
            -classpath "$ANDROID_JAR" \
            -d build/classes \
            java/io/battlewithbytes/ucb/*.java
          cd build/classes
          jar cf ../ucblib.jar io/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/ucblib.jar
          generate_release_notes: true
